<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFood_NNBinh"
        tools:targetApi="31">

        <!-- Màn hình chính khởi động -->
        <activity
            android:name=".LoginActivity_NNBinh"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các activity bạn đã tạo -->
        <activity android:name=".RegisterActivity_NNBinh" />
        <activity android:name=".HomeActivity_NNBinh" />
        <activity android:name=".FoodListActivity_NNBinh" />
        <activity android:name=".FoodDetailActivity_NNBinh" />

    </application>

</manifest>
